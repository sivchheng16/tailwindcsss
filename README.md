# tailwindcss install wite vite

1. npm crate vite@latest name.project
2. cd name.project
3. npm install tailwindcss @tailwindcss/vite
4. import tailwindcss from "tailwindcss/vite" in config.vite
5. @import "tailwindcss" into index.css in src

   ## Open project in the browser

   `npm run dev`
